import React from "react";
import "./Profile.css";

//TODO - Users need to be able to access profile from this page, and be prompted to login if not already logged in. Probably using firebase google auth.

//TODO - Get request with profile information (userid, name, bio, profile image, location)

function Profile() {
	return (
		<div>
			<h1>It Works</h1>
		</div>
	);
}

export default Profile;
